[pytest]
asyncio_mode = auto
markers =
    live: requires a running Auto_Upt deployment and may write test data
    external_publish: requires real platform credentials and may call external publishing APIs

